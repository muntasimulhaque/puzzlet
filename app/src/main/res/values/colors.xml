<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The resting ground, so the very first frame is already the app's
         world rather than a flash the Compose surface then replaces. -->
    <color name="paper">#FAF6EF</color>
    <!-- The adaptive icon's background: the lagoon teal the paper glyph sits
         on. Mirrored as IconDesign.TEAL in :tools, which regenerates the
         launcher PNGs. -->
    <color name="ic_launcher_background">#0C7A64</color>
</resources>
