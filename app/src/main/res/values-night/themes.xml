<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The app is always light: the night variant matches the day window so
         launch never flashes dark before Compose paints its first frame. -->
    <style name="Theme.Puzzlet" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/paper</item>
    </style>
</resources>
