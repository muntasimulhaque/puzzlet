<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: gives the screenshot harness a bare activity to host
     arbitrary game states. Never shipped: release builds do not merge it. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="false"
            android:theme="@style/Theme.Puzzlet" />
    </application>

</manifest>
